# /etc/conf.d/rtorrentd: config file for /etc/init.d/rtorrentd

# The username/uid to start rtorrent with
USER="p2p"

# If the arrow keys do not work, change it to "linux"
USED_TERM="linux"

# How many seconds to wait before send KILL signal
TERM_TIMEOUT=15
